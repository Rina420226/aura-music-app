name: aura_music
description: "Free modern cloud music player with AdMob integration."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.4
  just_audio: ^0.9.40
  google_mobile_ads: ^5.2.0

flutter:
  uses-material-design: true
